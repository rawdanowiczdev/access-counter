# dependencies
/node_modules

# logs are read-only files not a part of the app
/logs/*

# exception to keep /logs folder by itself
!logs/.gitkeep

# contains sensitive data
nodemon.json
ecosystem.config.js
